const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];
const header=$('#siteHeader'),progress=$('#progress'),nav=$('#nav'),menu=$('#menuButton');
window.addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>35);const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max?scrollY/max*100:0)+'%'},{passive:true});
menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});$$('#nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.15});$$('.reveal').forEach(el=>observer.observe(el));
const data={salud:['Centro de salud','Una pieza que acerca servicios esenciales al sector y estructura parte del funcionamiento del nodo.'],publico:['Cancha + espacio público','El espacio abierto articula recreación, encuentro, paisaje y recorridos como parte del equipamiento comunitario.'],cic:['Centro integrador comunitario','Espacios para formación, participación, organización y actividades colectivas del barrio.'],jardin:['Jardín maternal','Infraestructura de cuidado y primera infancia vinculada al resto de los servicios del nodo.']};
$$('.program-item').forEach(b=>b.addEventListener('click',()=>{$$('.program-item').forEach(x=>x.classList.remove('active'));b.classList.add('active');const [t,d]=data[b.dataset.key];$('#programNote').innerHTML=`<strong>${t}</strong><span>${d}</span>`}));
$$('.tab').forEach(b=>b.addEventListener('click',()=>{$$('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');const img=$('#viewerImage');img.style.opacity=.08;setTimeout(()=>{img.src=b.dataset.image;img.alt=b.dataset.title;$('#viewerLabel').textContent=b.dataset.title;$('#viewerStage').dataset.image=b.dataset.image;$('#viewerStage').dataset.caption=b.dataset.title;img.style.opacity=1},120)}));
$$('.system-head').forEach(h=>h.addEventListener('click',()=>{const body=h.nextElementSibling,open=body.classList.toggle('open');h.setAttribute('aria-expanded',open);h.querySelector('i').textContent=open?'−':'+'}));
const dlg=$('#lightbox'),dlgImg=$('#lightboxImage'),dlgCap=$('#lightboxCaption');
$$('.zoomable').forEach(b=>b.addEventListener('click',()=>{dlgImg.src=b.dataset.image;dlgImg.alt=b.dataset.caption||'Imagen ampliada';dlgCap.textContent=b.dataset.caption||'';dlg.showModal();document.body.style.overflow='hidden'}));
function closeBox(){dlg.close();document.body.style.overflow=''}$('#closeLightbox').addEventListener('click',closeBox);dlg.addEventListener('click',e=>{if(e.target===dlg)closeBox()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&dlg.open)closeBox()});
